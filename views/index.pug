extends layout.pug

block content
    h2 Welcome to Construction Portal

    div.col-md-6.col-md-offset-3.login-box
      div
          form(method='POST', action="/attendance")
            input.form-control.button(type="submit", value="MANAGE ATTENDANCE", id="go")
            br
            br
            br
      div
          form(method='POST', action="/searchUser")
            input.form-control.button(type="submit", value="ACCESS EXISTING USER", id="go")
